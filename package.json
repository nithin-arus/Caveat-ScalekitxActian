{
  "name": "caveat-scalekit-actian-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "web",
    "worker",
    "cron"
  ],
  "scripts": {
    "postinstall": "npm run build --workspace=web",
    "build": "npm run build --workspace=web",
    "start": "npm run start --workspace=web"
  }
}
